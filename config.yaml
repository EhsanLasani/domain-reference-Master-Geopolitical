database:
  host: localhost
  port: 5432
  name: referencemaster
  user: postgres
  password: "@Salman2021"
  ssl_mode: disable
  schema: domain_reference_master_geopolitical
  max_connections: 25
  conn_max_lifetime: 5m

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

server:
  host: 0.0.0.0
  port: 8081
  read_timeout: 30s
  write_timeout: 30s

auth:
  jwt_secret: "geopolitical-service-jwt-secret-key-2024"
  token_expiry: 24h
  refresh_expiry: 168h

tracing:
  enabled: true
  service_name: "geopolitical-service"
  endpoint: "http://localhost:14268/api/traces"

environment: development
log_level: debug